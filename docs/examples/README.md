# Synthetic Examples

This folder contains small, synthetic example outputs for documentation and testing. Do not place real customer or private data here.

Included:
- `sample_customers.csv`: tiny fake dataset demonstrating expected CSV structure.
- (Optional) `sample_orders.csv`: additional example.

Guidelines:
- Use invented names and values.
- Keep files small (<1MB) to avoid repo bloat.
- Do not export real data to this directory.
